@{
    ViewBag.Title = "Calendar";
}

@section Styles{
    <environment include="development">
            <link href="~/lib/fullcalendar/main.css" rel="stylesheet" />
            <script src="~/lib/fullcalendar/main.js"></script>
    </environment>
    <link ref="stylesheet" href="~/css/calendar.css" />
}

<div class="col-12 pt-2 bg-light" id="calendar-main">
    <div class="row">
        <div class="col-12 col-md-3 h-100">
            <div class="border border-dark">
                <div class="col-12 my-2">
                <h4>Calendars</h4>
                </div>
                <div class="col-12 my-2">
                    <div class="">Get Calendar By:</div>
                    <select v-model="selectedFilter" id="calendarFilter" name="calendarFilter" 
                            class="w-100 btn dropdown-toggle border border-dark">
                        <option value="" selected>Choose...</option>
                        <option v-for="option in filters" v-bind:value="option.value">
                            {{ option.text }}
                      </option>
                    
                    </select>
                </div>
                <div class="col-12 my-2"> 
                    <div class="">Select {{getSelectedFilterText}}:</div>
                    <select v-model="selectedFilterValue" class="w-100 btn dropdown-toggle border border-dark" 
                        id="filterValuesDropDown" disabled>
                        <option value="-1" selected>Choose...</option>
                      <option v-for="option in getFilterValueOptions" v-bind:value="option.id">
                        {{ option.name }}
                      </option>
                    </select>
                </div>
                <div class="col-12 my-2">
                    <button v-on:click="renderCalendar" class="btn btn-primary">Get Calendar</button>
                </div>
            </div>
            <div class="my-2 border border-dark">
                <div class="col-12 my-2">
                    <h4>Appointment Details</h4>
                </div>
                <div class="col-12">
                    <div class="my-2">Surgeon : </div>
                    <div class="my-2">Patient : </div>
                    <div class="my-2">Surgery : </div>
                    <div class="my-2">Theatre : </div>
                    <div class="my-2">Start Time : </div>
                    <div class="my-2">Duration : </div>
                    <div class="my-2">EndTime : </div>
                    <div class="my-2">Priority : </div>
                </div>
            </div>
            

        </div>
        <div class="col-12 col-md-9">
            <div id="calendar"></div>
        </div>
    </div>
</div>

@*<div id="deleteEventModal">             
      <h2>Event</h2>

      <p id="eventText"></p>

      <button id="deleteButton">Delete</button>
      <button id="closeButton">Close</button>
</div>*@

@section Scripts{
    <script src="~/lib/vue/dist/vue.global.js"></script>
    <script src="~/js/calendar/calendar-requests.js"></script>
    <script src="~/js/calendar/calendar.js"></script>
    <script src="~/js/calendar/calendar-main.js"></script>
}